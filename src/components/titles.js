import styled from "styled-components";

const BigTitle = styled.h1`
    color: #ffffff;
`;

const MedTitle = styled.h2`
    colot: #FFFFFF;
    text-align: right;
`;

const SmallTitle = styled.h3`

`;


export { BigTitle, MedTitle, SmallTitle };